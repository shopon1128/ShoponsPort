# Web開発ガイドライン(HTML / CSS / JavaScript / PHP)

方針: Webの失敗は画面に映らない場所で起きる。「ブラウザで表示できた」は、セキュリティ・アクセシビリティ・他の画面幅・不正な入力への挙動について何も証明しない。見えない品質を、規約と機械的な検証で担保する。

## セキュリティ(最優先・すべてに優先)

**外部入力(`$_GET` / `$_POST` / URL / フォーム / Cookie / ヘッダ / アップロードファイル)は、すべて攻撃者が自由に書き換えられる値として扱う。** 以下は利便性や納期を理由に緩めない:

- **SQL**: クエリへの値の埋め込みはプリペアドステートメント(PDO + プレースホルダ)のみ。文字列連結・補間でSQLを組み立てるコードは書かない。既存コードに見つけたら修正提案として報告する
- **XSS**: 外部由来の値を出力する時は必ずエスケープする。PHPは `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`、JSは `textContent` を使い、外部由来の文字列を `innerHTML` / `eval` / `Function` に渡さない
- **パスワード**: `password_hash()` / `password_verify()` を使う。自作ハッシュ・md5/sha1・平文保存は禁止
- **CSRF**: 状態を変える処理はPOSTにし、CSRFトークンを検証する
- **アップロード**: 拡張子ではなく内容(MIME・画像なら再エンコード)で検証し、保存先は公開ディレクトリ外、ファイル名はサーバ側で生成する
- **秘密情報**: APIキー・DB接続情報をコードに直書きしない。公開領域外の設定ファイルまたは環境変数に置く
- **バリデーションの本体はサーバ側**。クライアント側(JS・HTML属性)のチェックはUX向上であり、防御ではない

## HTML

- セマンティック要素(`header` `nav` `main` `section` `article` `footer` `button`)を使う。`div` / `span` は適切な要素が他に無い時の最後の手段
- `img` には `alt`(純装飾なら `alt=""`)、`input` には `label` を関連付ける
- 見出しは `h1`→`h2`→`h3` の階層を飛ばさない(見た目のサイズ調整はCSSで行う)
- クリックで動くものは、ページ遷移なら `a`、動作なら `button`。`div` に `onclick` を付けない
- `<html lang="ja">` と viewport メタタグを必ず入れる

## CSS

- レイアウトは Flexbox / Grid で組む。`position: absolute` はオーバーレイ・バッジ等の局所用途に限る
- 色・余白・フォントサイズは `:root` のカスタムプロパティ(CSS変数)に集約する(マジックナンバー禁止のCSS版)
- `!important` は禁止。効かない場合は詳細度の競合を調べて構造で解決する
- `z-index` は変数で段階管理する(`--z-modal: 100` 等)。`9999` のような場当たりの値を書かない
- クラス命名は BEM 風(`block__element--modifier`)など、1つの規則に統一する
- スマホ幅(375px)とPC幅(1280px)の両方で表示確認するまで「完成」と言わない

## JavaScript

- `const` を基本とし、再代入が必要な時のみ `let`。`var` は禁止
- 比較は `===` / `!==` のみ
- 非同期は async/await + try/catch。`fetch` は `response.ok` を確認する(fetchは404や500でthrowしない)
- DOM取得(`querySelector` 等)の結果は null の可能性を扱う(チェックするか、存在前提をコメントで明示)
- グローバル変数を作らない。モジュール(`type="module"`)かクロージャでスコープを閉じる
- 同種の要素が多数あるイベントは、個別登録ではなくイベント委譲(親で `event.target` を判定)を使う
- DOM要素への参照・検索結果は変数にキャッシュし、ループ内で毎回 `querySelector` しない

## PHP

- PHP 8+ を前提とし、ファイル先頭に `declare(strict_types=1);`、引数・戻り値に型宣言を付ける
- DBアクセスは PDO(`ERRMODE_EXCEPTION`)+プリペアドステートメントに統一する
- エラー表示は開発時のみ。本番相当の設定では `display_errors=Off` にしてログへ出す(エラーメッセージは攻撃者への情報源になる)
- 処理(上部)と出力(下部)を分離する。HTMLの中にクエリやビジネスロジックを書かない
- 純PHPファイルでは閉じタグ `?>` を書かない(末尾の空白出力事故を防ぐ)
- 文字列処理は `mb_` 系関数を使う(日本語=マルチバイト前提)。日付は `DateTime` を使う

## 命名規則

Web系はエコシステム標準に従う: JS/PHPの関数・変数は `camelCase`、クラスは `PascalCase`、定数は `UPPER_SNAKE`、CSSクラスは `kebab-case`(またはBEM)。共通CLAUDE.mdの命名表(`_member` / `a_arg` 等)とは意図的に異なる — Webでは標準からの逸脱の方がコストになるため。既存プロジェクトに規約がある場合はそちらを最優先。

## 検証(「完了」と言う前に)

1. ブラウザの開発者コンソールにエラー・警告がゼロであることを確認する
2. PHPは `php -l` で構文確認し、実際にリクエストを通して確認する。実行できない環境では「未検証」と報告する
3. DBを触るコードは、`' OR 1=1 --` のような値を入力したら何が起きるかを自己レビューする
4. フォームは正常値だけでなく「空・極端に長い値・想定外の型・HTMLタグ入り文字列(`<script>alert(1)</script>`)」を入れて挙動を確認する
5. スマホ幅とPC幅の2つで表示を確認する
6. 検証できなかった項目は、黙って省かず「未検証」として列挙する
